<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Include Spring Boot's default Logback configuration to support %clr -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />

    <!-- Define properties for reuse -->
    <property name="LOGS_PATH" value="logs" />
    <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%ex}" />
    <property name="FILE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <!-- Console appender configuration (uses Spring Boot's console appender) -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Application log file appender -->
    <appender name="ApplicationFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS_PATH}/application.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOGS_PATH}/archived/application-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Error log file appender - for errors only -->
    <appender name="ErrorFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS_PATH}/error.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOGS_PATH}/archived/error-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Security-related log appender -->
    <appender name="SecurityFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS_PATH}/security.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOGS_PATH}/archived/security-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Async appender for better performance -->
    <appender name="AsyncApplicationFile" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ApplicationFile" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- Async appender for error logs -->
    <appender name="AsyncErrorFile" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ErrorFile" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- Async appender for security logs -->
    <appender name="AsyncSecurityFile" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="SecurityFile" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- Logger configurations -->
    <!-- Security-related loggers -->
    <logger name="org.springframework.security" level="INFO" additivity="false">
        <appender-ref ref="Console" />
        <appender-ref ref="AsyncSecurityFile" />
        <appender-ref ref="AsyncErrorFile" />
    </logger>

    <logger name="com.secureops.security" level="INFO" additivity="false">
        <appender-ref ref="Console" />
        <appender-ref ref="AsyncSecurityFile" />
        <appender-ref ref="AsyncErrorFile" />
    </logger>

    <!-- Your application loggers -->
    <logger name="com.secureops" level="DEBUG" additivity="false">
        <appender-ref ref="Console" />
        <appender-ref ref="AsyncApplicationFile" />
        <appender-ref ref="AsyncErrorFile" />
    </logger>

    <!-- Spring framework loggers -->
    <logger name="org.springframework" level="INFO" additivity="false">
        <appender-ref ref="Console" />
        <appender-ref ref="AsyncApplicationFile" />
        <appender-ref ref="AsyncErrorFile" />
    </logger>

    <!-- Hibernate and database loggers -->
    <logger name="org.hibernate" level="INFO" additivity="false">
        <appender-ref ref="Console" />
        <appender-ref ref="AsyncApplicationFile" />
        <appender-ref ref="AsyncErrorFile" />
    </logger>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="Console" />
        <appender-ref ref="AsyncApplicationFile" />
        <appender-ref ref="AsyncErrorFile" />
    </root>
</configuration>