# Docker profile - disable SSL, use Docker networking
server.port=8080
server.ssl.enabled=false

# Trust forwarded headers from nginx proxy
server.forward-headers-strategy=framework

# PostgreSQL via Docker Compose service name
spring.datasource.url=jdbc:postgresql://db:5432/secureops
